# Simple linear regression model for demonstration
# This example assumes a model y = 2x + 1

def predict_price(feature_value):
    # In a real-world scenario, you would load a trained model
    # and use it to predict based on the input feature_value
    return 2 * feature_value + 1
